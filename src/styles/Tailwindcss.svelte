<style global>
  @tailwind base;

  body {
    @apply font-body;
  }

  h1 {
    @apply font-display font-bold text-6xl;
  }

  h2 {
    @apply font-display font-bold text-5xl;
  }

  h3 {
    @apply font-bold text-2xl;
  }

  h4 {
    @apply font-bold text-xl;
  }

  @tailwind components;
  @tailwind utilities;
</style>
